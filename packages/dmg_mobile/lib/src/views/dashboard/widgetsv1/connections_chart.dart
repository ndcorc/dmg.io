import 'package:flutter/material.dart';

class ConnectionsChart extends StatelessWidget {
  const ConnectionsChart({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(),
    );
  }
}
